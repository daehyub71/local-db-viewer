# Local DB Viewer

로컬 데이터베이스 파일을 조회하고 쿼리할 수 있는 경량 데스크톱 애플리케이션입니다. 폐쇄망/VDI 환경을 위해 PySide6(Qt6)로 개발되었습니다.

[English README](README.md)

## 주요 기능

- **다중 포맷 지원**: SQLite (.db, .sqlite, .sqlite3) 및 확장 가능한 커넥터 구조
- **데이터베이스 탐색기**: 테이블, 컬럼, 인덱스를 트리 뷰로 표시
- **스키마 뷰어**: 컬럼 타입, 제약조건, PK/FK, DDL 조회
- **데이터 뷰어**: 페이지네이션 지원 데이터 표시 및 컬럼 정렬
- **SQL 쿼리 편집기**: 구문 강조, 실행 시간 표시
- **쿼리 히스토리**: 검색 기능이 있는 영구 히스토리 저장
- **내보내기**: CSV, JSON 형식 지원
- **다크 테마**: VS Code 스타일의 모던한 다크 UI
- **포터블**: VDI/폐쇄망 환경을 위한 단일 EXE 배포

## 화면 구성

```
+----------------------------------------------------------+
| File  Edit  Query  View  Help                             |
+----------------------------------------------------------+
| [Open] [Refresh] | [Execute F5] [Cancel] | [Export]       |
+----------------------------------------------------------+
|                  |                                        |
| Database Tree    | [Schema] [Data] [Query] [History]      |
|                  |                                        |
| + sample.db      |  Column   | Type    | Nullable | PK   |
|   - users        |  ---------|---------|----------|------|
|   - orders       |  id       | INTEGER | NO       | YES  |
|   - products     |  name     | TEXT    | NO       |      |
|                  |                                        |
+----------------------------------------------------------+
| Connected: sample.db | Tables: 3 | Query: 0.05s          |
+----------------------------------------------------------+
```

## 설치 방법

### 사전 요구사항

- Python 3.9 이상
- PySide6

### 빠른 시작

```bash
# 저장소 클론
git clone https://github.com/daehyub71/local-db-viewer.git
cd local-db-viewer

# 가상환경 생성
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# 의존성 설치
pip install -r requirements.txt

# 애플리케이션 실행
python run.py
```

### 샘플 데이터베이스 생성 (선택사항)

```bash
python scripts/create_sample_db.py
```

`data/sample/sample.db`에 users, orders, products 테이블이 포함된 샘플 SQLite 데이터베이스가 생성됩니다.

## 사용 방법

1. **데이터베이스 열기**: "Open" 버튼 클릭 또는 `Ctrl+O`로 DB 파일 선택
2. **테이블 탐색**: 왼쪽 패널의 트리 뷰에서 테이블 클릭
3. **스키마 조회**: Schema 탭에서 컬럼 정의, 타입, 제약조건 확인
4. **데이터 조회**: Data 탭에서 페이지네이션된 데이터 탐색 (헤더 클릭으로 정렬)
5. **쿼리 실행**: Query 탭에서 SQL 작성 후 `F5`로 실행
6. **결과 내보내기**: "Export" 버튼으로 CSV 또는 JSON으로 저장

### 단축키

| 단축키 | 기능 |
|--------|------|
| `Ctrl+O` | 데이터베이스 열기 |
| `Ctrl+W` | 데이터베이스 닫기 |
| `F5` | 쿼리 실행 / 새로고침 |
| `Ctrl+Q` | 종료 |

## 포터블 EXE 빌드

폐쇄망/VDI 환경 배포용:

```bash
# PyInstaller 설치
pip install pyinstaller

# EXE 빌드
python scripts/build_exe.py
```

결과물: `dist/LocalDBViewer_Portable/`

포터블 패키지 구성:
- `LocalDBViewer.exe` - 메인 애플리케이션
- `data/` - 쿼리 히스토리 저장소
- `logs/` - 애플리케이션 로그
- `README.txt` - 빠른 시작 가이드

## 아키텍처

```
┌─────────────────────────────────────────────────────────────┐
│  Presentation Layer (PySide6)                              │
│  - MainWindow, DatabaseTree, SchemaViewer, DataViewer      │
│  - QueryEditor, HistoryViewer                              │
└─────────────────────────┬───────────────────────────────────┘
                          │
┌─────────────────────────┴───────────────────────────────────┐
│  Business Logic Layer                                       │
│  - BaseConnector (Abstract), SQLiteConnector                │
│  - ConnectorFactory, ExportService                          │
└─────────────────────────┬───────────────────────────────────┘
                          │
┌─────────────────────────┴───────────────────────────────────┐
│  Data Layer                                                 │
│  - QueryHistoryDB (SQLite)                                  │
└─────────────────────────────────────────────────────────────┘
```

### 확장 가능한 커넥터 구조

새로운 데이터베이스 포맷 추가 방법:

1. `BaseConnector`를 상속받는 새 커넥터 클래스 생성
2. 필수 메서드 구현: `connect()`, `get_tables()`, `get_schema()`, `execute_query()` 등
3. `ConnectorFactory`에 등록

```python
# 예시: DuckDB 지원 추가
class DuckDBConnector(BaseConnector):
    SUPPORTED_EXTENSIONS = ['.duckdb']

    def connect(self, file_path: str) -> bool:
        # 구현
        pass

# 등록
ConnectorFactory.register_connector('.duckdb', DuckDBConnector)
```

## 프로젝트 구조

```
local-db-viewer/
├── app/
│   ├── main.py                     # 진입점
│   ├── ui/                         # UI 컴포넌트
│   │   ├── main_window.py
│   │   ├── database_tree.py
│   │   ├── schema_viewer.py
│   │   ├── data_viewer.py
│   │   ├── query_editor.py
│   │   └── history_viewer.py
│   ├── core/connectors/            # 데이터베이스 커넥터
│   │   ├── base_connector.py
│   │   ├── sqlite_connector.py
│   │   └── connector_factory.py
│   ├── db/query_history.py         # 쿼리 히스토리 저장
│   └── utils/sql_highlighter.py    # SQL 구문 강조
├── resources/styles/dark_theme.qss # 다크 테마 스타일시트
├── scripts/
│   ├── build_exe.py                # PyInstaller 빌드 스크립트
│   └── create_sample_db.py         # 샘플 DB 생성기
├── run.py                          # 애플리케이션 실행기
├── LocalDBViewer.spec              # PyInstaller 설정
└── requirements.txt
```

## 의존성

| 패키지 | 버전 | 용도 |
|--------|------|------|
| PySide6 | ≥6.6.0 | Qt6 GUI 프레임워크 |
| PyInstaller | ≥6.3.0 | EXE 패키징 (선택사항) |

**참고**: `sqlite3`는 Python 표준 라이브러리에 포함되어 있습니다.

## 지원 데이터베이스 포맷

| 포맷 | 확장자 | 상태 |
|------|--------|------|
| SQLite | .db, .sqlite, .sqlite3 | ✅ 지원 |
| DuckDB | .duckdb | 🔜 예정 |
| MS Access | .mdb, .accdb | 🔜 예정 |

## 기여하기

1. 저장소 Fork
2. 기능 브랜치 생성 (`git checkout -b feature/new-feature`)
3. 변경사항 커밋 (`git commit -m 'Add new feature'`)
4. 브랜치에 Push (`git push origin feature/new-feature`)
5. Pull Request 생성

## 라이선스

MIT License - 자세한 내용은 [LICENSE](LICENSE) 파일을 참조하세요.

## 감사의 말

- [PySide6](https://doc.qt.io/qtforpython/) - Qt for Python
- [PyInstaller](https://pyinstaller.org/) - Python to EXE 패키징
